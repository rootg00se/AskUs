import { Button } from "@/shared/components/ui";
import type React from "react";

const App: React.FC = () => {
    return (
        <div className="wrap">
            <Button>Some button</Button>
        </div>
    );
};

export default App;
